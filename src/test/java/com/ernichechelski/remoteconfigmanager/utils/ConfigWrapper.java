package com.ernichechelski.remoteconfigmanager.utils;

import com.ernichechelski.remoteconfigmanager.Config;

public class ConfigWrapper {
    Config parameters;

    public ConfigWrapper(Config parameters) {
        this.parameters = parameters;
    }
}